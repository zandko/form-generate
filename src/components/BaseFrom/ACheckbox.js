import BaseForm from './BaseForm'

export default class ACheckbox extends BaseForm {
  constructor(h, formData, obj, vm, type) {
    super(h, formData, obj, vm, type)
    return this.initialize
  }
}
